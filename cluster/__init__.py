"""pi-cluster: provision and explore a Raspberry Pi cluster.

The ``cluster`` command-line tool (see :mod:`cluster.cli`) discovers the Pis on
your network, generates an Ansible inventory, and applies a chosen *mode*
(kubernetes today; other distributed-systems demos in future).
"""

__version__ = "0.1.0"
